"""Contexto `companies`: empresas cliente de una asesoría.

S1.6 entrega solo la **lista de solo lectura** (`GET /companies`) como superficie para probar la
matriz de permisos RBAC. El CRUD y el import del Excel de empresas son de S1.5.
"""
