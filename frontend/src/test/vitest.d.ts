// Hace visibles al typecheck los globales de vitest (describe/it/expect/vi...)
// que habilita `globals: true` en la config, sin restringir el resto de @types.
/// <reference types="vitest/globals" />
